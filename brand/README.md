# Brand source files

Original logo, favicon and mascot art. Keep the originals here — export
sized copies for the store, don't overwrite these.

| File | What it is | Notes |
|---|---|---|
| `Get.png` | Logo — round "GetBug'd" badge, bug over mint with a heavy black ring | 2000×2000 PNG, **no transparency** (solid mint square behind the circle). Made 2026-09-07. |

## Open items

- **No favicon file yet.** The favicon has to be its own export — at 32px the
  "GetBug'd" text in `Get.png` disappears. Crop to just the bug, or redraw it.
- **No mascot file yet.** Mascot #1 (the named roly-poly) is decided but not
  drawn. See the Decisions Log in `getbugd-internal`.
- **No vector (SVG) yet.** `Get.png` is pixels only, so it can't be scaled up
  clean for stickers, packaging or a big header.
- **Colors don't match the brand palette.** This logo is mint + green; the
  decided store palette is wheat `#F2E1B5`, hot pink `#E5197C`, rust `#8C3A0D`,
  cocoa `#1E1008`, with mint `#A6F0C6` held back for sales/events/packaging.
  Either the logo gets recolored or the palette decision changes — Nicholas's call.
- **No transparent version.** Needed before the logo can sit on a wheat section
  or a dark tank photo without a mint box around it.
